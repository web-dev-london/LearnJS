/* Recap
When installing packages, npm will save their names and versions in the package.json file.
dependencies lists the packages that an end-user of a package would need.
devDependencies lists the packages that a developer of a package would need.
 */

/* Recap
You can search for package names on npmjs.com.
You can browse popular packages by category on awesome-javascript.
We recommend local installations over global installations.
npx is a command that comes with npm and lets you execute packages installed locally. */
